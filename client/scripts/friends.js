var Friends = {
  friendsList: [],

  // On clicking a username in chats add this friend to the friendslist
  toggleStatus: function(userEvent) {
    console.log(userEvent);
    // Add to friendslist if not there
    // For this friend find each post and click to apply
  },

};
